# DiffusionPen — Python dependencies
# Tested with Python 3.12, CUDA 12.4
#
# Install torch first (CUDA 12.4 wheel — change cu124 to match your CUDA version):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# Then install the rest:
#   pip install -r requirements.txt

# --- Core deep learning ---
torch>=2.6.0           # install via pytorch.org wheel, not from here
torchvision>=0.21.0    # same
diffusers==0.36.0
transformers==5.0.0
accelerate==1.12.0
safetensors==0.5.0
tokenizers==0.22.2

# --- Model components ---
timm==1.0.20           # MobileNetV2 style encoder
einops==0.8.0

# --- Image processing ---
pillow>=10.0.0
opencv-python>=4.10.0
scikit-image>=0.25.0

# --- Data / numerics ---
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
pandas>=2.0.0

# --- Training utilities ---
tqdm>=4.65.0
wandb>=0.17.0
tensorboard>=2.14.0

# --- HuggingFace / model downloads ---
huggingface_hub>=0.20.0

# --- Misc ---
matplotlib>=3.7.0
PyYAML>=6.0
omegaconf>=2.3.0
